// idea-storage config
// Place this file at: ~/.config/idea-storage/config.ts

export default {
  // Directories to scan for Claude Code session JSONL files
  // claudeDirs: ["~/.claude", "~/.claude-work"],
  // Minimum session age in minutes before processing (default: 120)
  // minAgeMinutes: 120,
};
